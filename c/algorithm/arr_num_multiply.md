```Algorithm
Algorithm multiplyarray(A,n,num)
A is an array with size n and num is a number to multiply with the array:
{
     for i:= 1 to n do
         product := A[i] * num;

     return 0;
}
```
// big o = n + 1+1 = n+2 = n =>O(n)
